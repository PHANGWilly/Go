- switch case :
    - `operatingSystem` peut-être obsolète
```Go
    switch operatingSystem {
        case "Windows":
            // if Windows
        case "MacOS":
            // if MacOS
        case "Linux":
            // if Linux
        default:
            // else
    }
```